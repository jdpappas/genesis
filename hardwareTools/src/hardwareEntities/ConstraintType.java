package hardwareEntities;
/**
 * The enumeration defines the possible types a Constraint can be.
 * UPPER for an upper limit.
 * LOWER for a lower limit.
 * STATE if a constraint is of type ON/OFF.
 * @author John
 *
 */
public enum ConstraintType {
	UPPER, LOWER, STATE;
}
